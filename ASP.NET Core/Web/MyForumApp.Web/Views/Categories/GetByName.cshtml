@model MyForumApp.Web.ViewModels.Categories.CategoryViewModel
@{
    this.ViewData["Title"] = Model.Title;
}

<h1 class="display-3">@Model.Title</h1>
<div class="alert alert-info">
    @Model.Description
</div>

<hr />

<div>
    <a href="/Posts/CreatePost" type="button" class="btn btn-primary" />Add a new post<a />
    <div class="dropdown position-relative" id="dd">
        <button type="button" data-boundary="window" class="btn btn-primary dropdown-toggle" data-toggle="dropdown">
            Dropdown
        </button>
        <div class="dropdown-menu">
            <a class="dropdown-item" data-boundary="window" href="#">Link 1</a>
            <a class="dropdown-item" data-boundary="window" href="#">Link 1</a>
            <a class="dropdown-item" data-boundary="window" href="#">Link 1</a>
        </div>
    </div>

    <div class="dropdown">
        <button class="btn btn-primary dropdown-toggle" data-boundary="dd" type="button" data-toggle="dropdown">
            Sort by
            <span class="caret"></span>
        </button>
        <ul class="dropdown-menu">
            <li><a href="#" id="drop1" data-toggle="dropdown" data-flip="false">Date</a></li>
            <li><a href="#" id="drop2" data-toggle="dropdown" data-flip="false">Comments</a></li>
            <li><a href="#" id="drop3" data-toggle="dropdown" data-flip="false">Votes</a></li>
        </ul>
    </div>

    <div class="dropdown position-relative" id="dd">
        <button type="button" data-boundary="dd" class="btn btn-primary dropdown-toggle" data-toggle="dropdown">
            Dropdown
        </button>
        <div class="dropdown-menu">
            <a class="dropdown-item" href="#">Link 1</a>
            <a class="dropdown-item" href="#">Link 1</a>
            <a class="dropdown-item" href="#">Link 1</a>
            <a class="dropdown-item" href="#">Link 1</a>
            <a class="dropdown-item" href="#">Link 1</a>
            <a class="dropdown-item" href="#">Link 1</a>
            <a class="dropdown-item" href="#">Link 1</a>
            <a class="dropdown-item" href="#">Link 1</a>
            <a class="dropdown-item" href="#">Link 1</a>
            <a class="dropdown-item" href="#">Link 1</a>
            ...
        </div>
    </div>

</div>


@*<div class="dropdown">
    <button class="btn btn-primary dropdown-toggle" type="button" data-toggle="dropdown">
        Sort by
        <span class="caret"></span>
    </button>
    <ul class="dropdown-menu">
        <li><a href="#">Date</a></li>
        <li><a href="#">Comments</a></li>
        <li><a href="#">Votes</a></li>
    </ul>
</div>*@

<hr />

@foreach (var post in Model.ForumPosts)
{
    <div class="container-fluid mt-100">
        <div class="row">
            <div class="col-md-12">
                <div class="card mb-4">
                    <div class="card-header">
                        <div class="media flex-wrap w-100 align-items-center">
                            <div class="media-body ml-3">
                                <h4 class="media-heading"><a asp-controller="Posts" asp-action="ById" asp-route-id="@post.Id">@post.Title</a></h4>
                                <input asp-for="@Model.Title" type="hidden" class="text-danger" />
                                <div class="text-muted small">
                                    <time datetime="@post.CreatedOn"></time>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="card-body">
                        <article>
                            <a asp-controller="PostDetails" asp-action="Details" asp-route-postId="@post.Id" asp-route-categoryId="@Model.Id">Details</a>
                            <input asp-for="@Model.Title" type="hidden" class="text-danger" />
                            <input asp-for="@Model.Id" type="hidden" class="text-danger" />
                        </article>
                    </div>
                </div>
            </div>
        </div>
        <ul class="list-inline list-unstyled text-right">
            <li class="list-inline-item">
                <i class="fas fa-user"></i> @post.UserUserName
            </li>
            <li class="list-inline-item">
                <i class="far fa-calendar-alt"></i> @post.CreatedOn
            </li>
            <li class="list-inline-item">
                <i class="fas fa-comment-dots"></i> @post.CommentsCount comments
            </li>
        </ul>
    </div>
}

<hr />
<div>
    <a asp-controller="Home" asp-action="Index" type="button" class="btn btn-primary" />Back to the Homepage<a />
</div>

<nav>
    <ul class="pagination justify-content-center">
        @if (this.Model.CurrentPage == 1)
        {
            <li class="page-item disabled">
                <a class="page-link" href="#">Previous</a>
            </li>
        }
        else
        {
            <li class="page-item">
                <a class="page-link"
                   asp-route="forumCategory"
                   asp-route-name="@this.Model.Title"
                   asp-route-page="@(this.Model.CurrentPage - 1)">Previous</a>
            </li>
        }

        @for (int i = 1; i <= this.Model.PageCount; i++)
        {
            var active = i == this.Model.CurrentPage ? "active" : string.Empty;
            <li class="page-item @active">
                <a class="page-link"
                   asp-route="forumCategory"
                   asp-route-name="@this.Model.Title"
                   asp-route-page="@i">@i</a>
            </li>
        }

        @if (this.Model.CurrentPage == this.Model.PageCount)
        {
            <li class="page-item disabled">
                <a class="page-link" href="#">Next</a>
            </li>
        }
        else
        {
            <li class="page-item">
                <a class="page-link"
                   asp-route="forumCategory"
                   asp-route-name="@this.Model.Title"
                   asp-route-page="@(this.Model.CurrentPage + 1)">Next</a>
            </li>
        }
    </ul>
</nav>
